apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pippo.fullname" . }}-test-connection"
  labels:
    {{- include "pippo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pippo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
